#include <iostream>
#include <vector>
#define FASTIO \
cin.tie(NULL); \
cout.tie(NULL); \
ios::sync_with_stdio(false);

using namespace std;

void solve(){
    int n,x,k,a; cin >> n >> x>> k;
    for(int i = 0; i < n; i++){
        cin >> a;
        if(a){

        } else {
            
        }
    }
}

int main(void){
    int t = 1;
    for(int i = 0; i< t; i++) solve();
    return 0;
}
